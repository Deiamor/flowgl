{
  "name": "flowgl-monorepo",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter demo dev",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm --filter @flowgl/core test && pnpm --filter '@flowgl/react' test && pnpm --filter '@flowgl/vue' test && pnpm --filter '@flowgl/svelte' test",
    "test:wrappers": "pnpm --filter '@flowgl/react' test && pnpm --filter '@flowgl/vue' test && pnpm --filter '@flowgl/svelte' test",
    "test:coverage": "pnpm --filter @flowgl/core exec vitest run --coverage"
  },
  "devDependencies": {
    "playwright": "^1.49.0",
    "typescript": "^5.4.0"
  }
}
